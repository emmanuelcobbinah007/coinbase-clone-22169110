# Example env file for local development
# Set the API base URL for the backend. Vite reads variables prefixed with VITE_.
# Example values (copy this file to `.env` and update as needed):
VITE_API_BASE=http://localhost:5000/api/crypto
# Dev server proxy (used by Vite dev server).
VITE_DEV_PROXY=http://localhost:5000
